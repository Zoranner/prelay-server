{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Provider Relay desktop window capability.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
