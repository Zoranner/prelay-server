services:
  prelay-server:
    image: ghcr.io/zoranner/prelay-server:0.1.0
    container_name: prelay-server
    restart: always
    env_file:
      - .env
    ports:
      - "18080:18080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config:ro
networks:
  default:
    external: true
    name: prelay
