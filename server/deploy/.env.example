# Required. Generate once and keep this value stable after deployment.
PROVIDER_RELAY_MASTER_KEY=REPLACE_WITH_A_BASE64_ENCODED_32_BYTE_KEY

# Optional runtime settings.
LISTEN_PORT=18080
RUST_LOG=provider_relay_server=info,tower_http=info
MODEL_PRICES_PATH=config/model_prices.json
