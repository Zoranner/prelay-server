meta {
  name: 重新生成接口令牌
  type: http
  seq: 18
}

post {
  url: {{relay_url}}/api/endpoints/:endpoint_id/regenerate-token
  body: none
  auth: bearer
}

auth:bearer {
  token: <device-credential>
}

params:path {
  endpoint_id: <endpoint-id>
}
