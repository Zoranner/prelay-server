meta {
  name: Rotate credential
  type: http
  seq: 2
}

post {
  url: {{relay_url}}/api/identity/credential/rotate
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{device_credential}}
}

body:json {
  {
    "new_credential": "{{new_device_credential}}"
  }
}
